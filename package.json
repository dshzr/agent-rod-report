{
  "name": "agent-rod-report",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsx src/langchain/server.ts",
    "dev:web": "npm run dev --prefix web",
    "dev:all": "npx concurrently -k -n agent,web -c blue,green \"npm run dev\" \"npm run dev:web\"",
    "build": "tsc --noEmit",
    "build:web": "npm run build --prefix web",
    "start": "tsx src/langchain/server.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "dependencies": {
    "@langchain/langgraph": "^1.0.0",
    "@langchain/openai": "^1.2.0",
    "dotenv": "^16.4.7",
    "langchain": "^1.5.4",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.20.3",
    "typescript": "^5.9.3"
  }
}
